import { createSlice } from '@reduxjs/toolkit'; import buyers from '@/data/buyers.json'; import businesses from '@/data/businesses.json'; const initialState = { buyerCards: buyers, businessCards: businesses, myMatches: [], saved: [] }; const matchesSlice = createSlice({ name:'matches', initialState, reducers:{ acceptCard:(s,a)=>{ const {type,id} = a.payload; s.myMatches.push({type,id}) }, rejectCard:()=>{}, saveCard:(s,a)=>{ s.saved.push(a.payload) } } }); export const { acceptCard, rejectCard, saveCard } = matchesSlice.actions; export default matchesSlice.reducer;