import { useState } from 'react'
export default function BusinessProfileModal({ biz, onClose, onStartDeal, onSendMessage }){ const [msg,setMsg]=useState("Hi, I'm interested. Can we schedule a call?"); if(!biz) return null; return (<div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50"><div className="bg-white rounded-xl2 shadow-soft p-6 w-full max-w-2xl"><div className="flex justify-between items-center mb-3"><div className="text-lg font-semibold">{biz.name}</div><button onClick={onClose} className="text-gray-500">✕</button></div><div className="grid md:grid-cols-2 gap-4 text-sm"><div><div className="mb-2"><span className="font-medium">Industry:</span> <span className="capitalize">{biz.industry}</span></div><div className="mb-2"><span className="font-medium">Location:</span> {biz.location}</div><div className="mb-2"><span className="font-medium">Revenue:</span> {biz.revenue}</div><div className="mb-2"><span className="font-medium">Asking Price:</span> {biz.price}</div></div><div><div className="font-medium mb-1">AI Summary (stub):</div><p className="bg-gray-50 p-3 rounded-xl2">Stable YoY revenue, strong gross margins, low churn SaaS-like retention. Risks: key-person dependency, regional concentration.</p><div className="text-xs text-gray-500 mt-1">Detailed docs locked until NDA.</div></div></div><div className="mt-4"><div className="font-medium mb-1">Message Seller</div><div className="flex gap-2"><input className="flex-1 border rounded-xl2 p-2" value={msg} onChange={e=>setMsg(e.target.value)} /><button className="btn bg-white border" onClick={()=>onSendMessage(msg)}>Send</button></div></div><div className="mt-4 text-right"><button className="btn bg-white border mr-2" onClick={onClose}>Close</button><button className="btn bg-teal text-white" onClick={onStartDeal}>Start Deal (Create NDA stage)</button></div></div></div>) }