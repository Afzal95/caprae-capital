import { useDispatch } from 'react-redux'; import { updateInfo } from '@/redux/userSlice'; import { useRouter } from 'next/router'; import OnboardingForm from '@/components/OnboardingForm'
export default function SellerOnboarding(){ const dispatch = useDispatch(); const router = useRouter(); const fields=[{name:'businessName',label:'Business Name',required:true},{name:'industry',label:'Industry',required:true},{name:'location',label:'Location',required:true},{name:'revenue',label:'Revenue',required:true},{name:'employees',label:'Employees',required:true},{name:'price',label:'Asking Price / Valuation',required:true},{name:'docs',label:'Upload Financial Docs (stub)',required:false,type:'text'},{name:'motivation',label:'Motivation for Selling',required:false,type:'textarea'},{name:'confidentiality',label:'Confidentiality Preferences',required:false,type:'textarea'}]; const onSubmit=(e)=>{e.preventDefault(); const form=Object.fromEntries(new FormData(e.target).entries()); dispatch(updateInfo(form)); router.push('/matches')}; return <OnboardingForm title="Seller Onboarding" fields={fields} onSubmit={onSubmit} /> }