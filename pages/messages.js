import Layout from '@/components/Layout'; import { useSelector } from 'react-redux'; import MessageThread from '@/components/MessageThread'; export default function Messages(){ const deals = useSelector(s=>s.deals.deals); const first = deals[0]; return (<Layout><div className="mb-4 text-xl font-semibold">Messages</div>{!first ? (<div className="card">No conversations yet.</div>) : (<MessageThread deal={first} />)}</Layout>) }